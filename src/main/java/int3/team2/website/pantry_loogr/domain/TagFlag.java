package int3.team2.website.pantry_loogr.domain;

/**
 * TagFlag splits up the tags attached to recipes into three categories, they are handled differently depending on them
 */
public enum TagFlag {
    GENERAL, CUISINE, ALLERGEN
}
